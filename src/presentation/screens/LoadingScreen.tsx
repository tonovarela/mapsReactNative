import React from 'react';
import { Text, View } from 'react-native'

export const LoadingScreen = () => {    
    return (
        <View style={{marginTop:50}}>
            <Text style={{color:'red'}}>LoadingScreen</Text>
        </View>
    )
}