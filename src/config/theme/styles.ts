import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
    btnPrimary:{
        backgroundColor:'black',
        padding:20,
        borderRadius:100,
        margin:20
    }
    
});